using UnityEngine;
using System.Collections;
using System;

public class Attack : MonoBehaviour {

    // Use this for initialization
    void getAttack() {

    }

    void Start () {
        string cardName = "";
        getAttack(cardName);
	}

    private void getAttack(object cardName)
    {
        throw new NotImplementedException();
    }

    // Update is called once per frame
    void Update () {
	
	}
}
